export enum Path {
  NONE = "#",
  LANDING = "/",
  LOGIN = "/login",
  REGISTER = "/register",
  SHOP = "/shop",
  PAYMENT = "/payment",
  PROFILE = "/profile",
}

export enum Endpoint {
  LOGIN = "/auth/login",
  REGISTER = "/auth/register",
  ME = "/auth/me",
}
