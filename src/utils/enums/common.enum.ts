export enum Path {
  NONE = "#",
  LANDING = "/",
  LOGIN = "/login",
  LOGOUT = "/logout",
  REGISTER = "/register",
  SHOP = "/shop",
  PAYMENT = "/payment",
  PROFILE = "/profile",
}

export enum Endpoint {
  LOGIN = "/auth/login",
  REGISTER = "/auth/register",
  ME = "/auth/me",
}

export enum MenuType {
  HAMBURGER = "hamburger",
  SUB_MENU = "sub-menu",
}
