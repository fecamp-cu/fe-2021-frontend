export enum PRODUCT_TYPE {
  oldPapers = "old_papers",
  examPreps = "exam_preps",
}

export enum PaymentTypes {
  eBank = "internet_banking",
  card = "credit_card",
  promptPay = "promptpay",
  eBankEndpoint = "internet-banking",
  cardEndpoint = "credit-card",
  promptPayEndpoint = "promptpay",
}
